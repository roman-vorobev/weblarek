# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:

- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:

- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск

Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```

## Сборка

```
npm run build
```

или

```
yarn build
```

# Интернет-магазин «Web-Larёk»

«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component

Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`

#### Класс Api

Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(url: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(url: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter

Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` - хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

### Данные

Здесь будет информация о данных для использования в проекте.

#### Сущность «Товар»

Предназначение данных для товара нужно для отображения в странице актуальной информации. Товар будет состоять из следующих полей:

export interface IProduct {
id: string;
title: string;
description: string;
price: number | null;
image: string;
category: string;
}

id - Идентификатор товара, где часто будем использовать этот ключ в методах, например: посмотреть модальное окно товара, добавить товар в корзину и т.д.
title - Название продукта. Поле нужно для отображения в интерфейсе пользователя (UI)
description - Описание товара. Поле нужно для отображения в интерфейсе пользователя (UI)
price - Цена товара. Нужно для отображения цены в интерфейсе пользователя (UI) и для работы доступности кнопки добавления в корзину
image - Изображение товара. Принимаем ссылку на изображение. Поле нужно для отображения в интерфейсе пользователя (UI).
category - Тип товара. Поле нужно для отображения в интерфейсе пользователя (UI)

#### Сущность «Корзина»

Корзина будет состоять из следующих полей. Нужно для использования ордера на покупку товаров

export interface ICustomer {
payment: TPayment | null;
email: string;
phone: string;
address: string;
}

payment - Способ оплаты. Принимаем значения "card" или "online" в type TPayment
email - Почта покупателя. Нужно для отправки чека покупки покупателю на почту, если выбран выриант покупки "При получении"
phone - Номер телефона покупателя. Нужно для связи с покупателем. Нужно для заполнения, если выбран выриант покупки "При получении"
address - Адрес доставки товаров. Нужно для заполнения, если выбран выриант покупки "Онлайн"

#### Сущность «API»

Предназначение получение и изменение данных с помощью методов get / post

export interface IApi {
get<T extends object>(url: string): Promise<T>;
post<T extends object>(
url: string,
data: object,
method?: ApiPostMethods,
): Promise<T>;
}

get<T extends object>(url: string): Promise<T> - Метод получения товаров по ссылке
post<T extends object> - Метод изменений данных
url: string - Ссылка, куда отправляет запрос
data: object - Тело запроса
method?: ApiPostMethods - Метод запроса

export interface IProductResponse {
total: number;
items: IProduct[];
}

total - Общее количество товаров на сервере
items - Массив товаров, соответствующих интерфейсу

export interface IOrder extends ICustomer {
total: number;
items: string[];
}

total - Итоговая сумма заказа
items - Массив строк (строго ID выбранных продуктов)

export interface IOrderResult {
id: string;
total: number;
}

id - Уникальный ID созданного заказа на сервере
total - Сумма, которая была списана

### Модели данных

#### Класс Catalog

Содержит хранение товаров, которые можно купить в приложении.

Конструктор:  
`constructor(initialProducts: IProduct[])` - В конструктор передается готовый список товаров для отображения на странице

Поля класса:  
`products: string[]` - Хранит список идентификаторов (ID) продуктов.
`selectedProduct: string` - Идентификаторов (ID) товара, выбранного для подробного отображения в модальном окне.

Методы:

`saveProducts(products: IProduct[]):void` - Принимает и сохраняет массив товаров полученного в параметрах метода в внутренне хранилище класса.
`getProducts():IProduct[]` - Возвращает массив объектов товаров по модели для отрисовки страницы.
`getProductByID(id: string):IProduct | undefined` - Принимает на вход id товара. Возвращает объект товара, если он был найден.
`saveProduct(id: string):void` - Принимает на вход id товара. Сохраняет id товара, который пользователь выбрал для подробного отображения.
`getSelectedProduct():IProduct | undefined` - Возвращает товар для отображения в модальном окне.

#### Класс Bucket

Содержит хранение товаров, которые пользователь выбрал для покупки.

Конструктор:  
`constructor(selectedProduct: IProduct[])` - В конструктор передается список продуктов, которые выбрал пользователь.

Поля класса:  
`selectedProducts: IProduct[] | null` - Хранит список выбранных продуктов

Методы:

`getBucketProducts():IProduct[]` - Возвращает список товаров, которые находятся в корзине
`setSelectedProductIntoBucket(product:IProduct):void` - Принимает и добавляет товар в корзину
`deleteSelectedProductFromBucket(ID: string):void` - Принимает ID продукта и удаляет товар с корзины
`deleteAllSelectedProductsFromBucket():void` - Очищает всю корзину после покупки
`getFullBucketPrice():number` - Возвращает общую сумму корзины
`getQuantityBucketProducts():number` - Возвращает количество товаров
`getProductInBucket(id:string):boolean` - Принимает ID продукта и возвращает ответ о его наличии в корзине

#### Класс Customer

Содержит данные покупателя, которые тот должен указать при оформлении заказа.

Конструктор:  
`constructor(customer: ICustomer)` - В конструктор передается объект с данными покупателя

Поля класса:  
`private payment: TPayment` - Вид оплаты: наличными или картой
`private email: string` - Почта покупателя
`private phone: string` - Номер телефона покупателя
`private address: string` - Адрес доставки покупателя

Методы:

`getAllCustomerData():ICustomer` - Получает данные о покупателе
`deleteAllCustomerData():void` - Удаляет данные о покупателе после покупки
`validateForm():Record<keyof ICustomer, string>` - Проверяет валидность введенных данных: номер телефона или почты
`setInputData(field: keyof ICustomer, value: string | TPayment):void` - Сохраняет введенные данные пользователем

### Слой коммуникации

#### Класс ApiModal

С помощью класса мы будем получать и изменять полученные данные

Конструктор:  
`constructor(api: IApi)` — Принимает экземпляр базового класса `Api` для выполнения сетевых запросов.

Поля класса:

`private api: IApi` - Запрос с параметрами

Методы:
`getProductsList(): Promise<IProductResponse>` — Выполняет GET-запрос на эндпоинт `/product/`, возвращая объект с массивом товаров.
`postOrder(order: IOrder): Promise<IOrderResult>` — Выполняет POST-запрос на эндпоинт `/order/`, передавая данные о заказе (товары + данные покупателя), и возвращает результат успешной покупки с ID заказа и суммой.

### Слой представления (View)

Слой представления (View) — это компонент архитектуры программного обеспечения, который отвечает за отображение данных пользователю и организацию взаимодействия с ним.

#### Класс BucketItemView

Отображает конкретную карточку в корзине

Поля класса

`private _index: HTMLElement` - Порядковый номар товара в корзине
`private _title: HTMLElement` - Наименование товара в корзине
`private _price: HTMLElement` - Цена товара в корзине
`private _deleteButton: HTMLButtonElement` - Кнопка удаления товара в корзине
`private _id!: string` - Идентификатор товара. Нужно для отрабатывания событий

Методы класса
`set id(value: string)` - Метод установки ID

`set index(value: number)` - Метод установки Индекса

`set title(value: string)` - Метод установки наименования товара

`set price(value: number | null)` - Метод установки цены товара

События класса

`basket:delete-item` - событие удаления карточки с корзины

#### Класс BucketView

Отображает корзину

Поля класса

`private _list: HTMLElement` - Список товаров
`private _price: HTMLElement` - Общая сумма всех товаров
`private _button: HTMLButtonElement` - Кнопка корзины в щапке страницы

Методы класса
`set items(value: HTMLElement[])` - Метод вывода сообщения при пустой корзине

`set total(value: number)` - Метод получения общей суммы

События класса

`basket:order` - Событие открытия корзины

#### Класс cardCatalogViev

Отображает конкретную карточку в списке товаров

Поля класса

`private _title: HTMLElement` - Наименование товара
`private _image: HTMLImageElement` - Рисунок товара
`private _category: HTMLElement` - Категория товара
`private _price: HTMLElement` - Цена товара
`protected _id!: string` - ID товара. Нужен для отработки событий

Методы класса
`set title(value: string)` - Метод установки наименования карточки

`set image(value: string)` - Метод установки рисунка товара

`set category(value: string)` - Метод установки категории товара

`set price(value: number | null)` - Метод установки цены товара

События класса

`card:select` - событие открытия модального окна карточки товара

#### Класс cardPreview

Отображает модальное окно карточки. Является наследником класса cardCatalogView

Поля класса

`private _text: HTMLElement` - Описание корточки в модальном окне
`private _button: HTMLButtonElement` - Кнопка "Купить" или "Удалить из корзины"
`private _productPrice: HTMLElement` - Цена товара

Методы класса
`set text(value: string)` - Метод установки описания товара карточки

`set price(value: number | null)` - Метод установки цены товара

`set isInBucket(value: boolean)` - Метод проверки наличие товара в корзине

События класса

`preview:basket-toggle` - событие добавления или удаления товара из корзины

#### Класс catalogView

Управляет всей главной витриной сайта

Поля класса

`private galleryContainer: HTMLElement` - Витрина
`private template: HTMLTemplateElement` - Карточки
`private events: IEvents` - События

Методы класса
`renderCatalog(products: IProduct[])` - Рендер витрины

#### Класс contactsView

Отображает модальное окно адреса и телефона

Поля класса

`private _emailInput: HTMLInputElement` - Ввод почты
`private _phoneInput: HTMLInputElement` - Ввод телефона
`private _submitButton: HTMLButtonElement` - Кнопка подтверждения данных и заказа
`private _formErrors: HTMLElement` - Отображение ошибки при невалидных значениях

Методы класса
`setErrors(errors: IContactsErrors)` - Метод отображения ошибок

События класса

`contacts:input` - событие проверки валидности данных

`contacts:submit` - событие подтверждения заказа

#### Класс modalView

Управляет состоянием модального окна

Поля класса

`private _closeButton: HTMLButtonElement` - Закрытия модального окна
`private _content: HTMLElement` - Отображение информации в модальном окне

Методы класса
`set content(value: HTMLElement)` - Метод управления данными в модальном окне

`open()` - Открытие модального окна

`close()` - Закрытие модального окна

#### Класс orderSuccessView

Отображает модальное окно успешного заказа

Поля класса

`private _description: HTMLElement` - Описание текста успешной покупки
`private _closeButton: HTMLButtonElement` - Кнопка закрытия модального окна

События класса

`success:close` - событие закрытия модального окна

#### Класс orderView

Модальное окно способа оплаты и адреса доставки

Поля класса

`private _cardButton: HTMLButtonElement` - Кнопка выбора оплаты картой
`private _cashButton: HTMLButtonElement` - Кнопка выбора оплаты наличными
`private _addressInput: HTMLInputElement` - Кнопка ввода адреса
`private _submitButton: HTMLButtonElement` - Кнопка перехода на следующий шаг
`private _formErrors: HTMLElement` - Такст отображения ошибок

Методы класса
`setErrors(errors: IOrderErrors)` - Метод отображения ошибок

События класса

`order:input` - Событие выбора способа оплаты и ввода адреса

`order:submit` - Событие перехода на следующий шаг

### Presenter

Задача презентера — инициализировать слои данных (Model) и отображения (View), а также связать их между собой с помощью событийной модели (`EventEmitter`).

#### Инициализируемые компоненты и сущности

В презентере создаются центральные экземпляры классов:

Слой данных (Models):
`Catalog` — модель для хранения и управления списком товаров.  
`Bucket` — модель корзины (добавление, удаление, подсчет стоимости).
`Customer` — модель данных покупателя (адрес, контакты, валидация форм).

Слой инфраструктуры и API:
`Api` и `ApiModal` — осуществляют сетевые запросы к серверу (получение товаров, отправка заказа).
`EventEmitter` — брокер событий для реализации слабосвязанной архитектуры.

Слой отображения (Views):
`CatalogView` — контейнер для отображения каталога товаров на главной странице.
`modalView` — глобальное модальное окно для вывода динамического контента.

#### Схема обработки событий

Презентер координирует следующие ключевые группы событий:

Работа с каталогом и карточками
`card:select` — открывает модальное окно с детальным описанием товара (`CardPreview`). Внутри проверяется, находится ли товар уже в корзине.

`preview:basket-toggle` — добавляет товар в корзину или удаляет его оттуда при клике на кнопку в карточке просмотра, после чего обновляет счетчик на главной странице.

#### Управление корзиной

`basket:open` — собирает DOM-элементы товаров в корзине (`BucketItemView`), инициализирует общую корзину (`BucketView`) со стоимостью и выводит её в модальное окно.

`basket:delete-item` — удаляет конкретный товар из модели корзины, обновляет счетчик и перезапускает событие открытия для актуализации UI.

`basket:order` — переводит пользователя к первому шагу оформления заказа.

#### Оформление заказа и валидация

`order:open` / `contacts:open` — инициализируют соответствующие формы заказа (`OrderView` / `ContactsView`), проводят первичную валидацию полей и рендерят их в модальное окно.

`order:input` / `contacts:input` — реагируют на ввод данных пользователем, сохраняют информацию в модель `Customer` и динамически передают ошибки валидации обратно в форму.

`order:submit` — переводит со страницы выбора способа оплаты на страницу контактов.

`contacts:submit` — собирает полные данные из моделей `Customer` и `Bucket`, отправляет POST-запрос на сервер. При успехе триггерит закрытие и очистку данных.

#### Успешное завершение

`success:modal-open` — очищает модель корзины и данные покупателя, сбрасывает счетчик UI и отображает окно успешной оплаты (`OrderSuccessView`) с итоговой суммой.

`success:close` — закрывает финальное модальное окно.

#### Инициализация приложения

При запуске приложения вызывается асинхронная функция `startApp()`, которая:

1. Запрашивает список товаров с сервера через `apiModal`.
2. Сохраняет полученные данные в модель `Catalog`.
3. Передает список товаров в `catalogView` для их первичной отрисовки на главной странице.
